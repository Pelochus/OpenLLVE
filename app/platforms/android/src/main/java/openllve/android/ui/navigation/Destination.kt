package openllve.android.ui.navigation

/**
 * Top-level destinations for the app. Kept as plain strings (Compose
 * navigation) rather than a framework abstraction.
 */
object Destination {
    const val HOME = "home"
    const val SETTINGS = "settings"
    const val IMAGE_RESULT = "image_result"
    const val VIDEO_RESULT = "video_result"
}
